import React from 'react'

const SingingButton = (props) => (
    <div>
        <button
            onClick={() => alert('lalalal')}
        >
            Label
        </button>
    </div>
)

export default SingingButton